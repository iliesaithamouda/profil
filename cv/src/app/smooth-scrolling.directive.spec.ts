import { SmoothScrollingDirective } from './smooth-scrolling.directive';

describe('SmoothScrollingDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
